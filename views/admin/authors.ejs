<!DOCTYPE html>
<html lang="en">
   <%- include('../partials/head', {title: "Автори"}); %>
   <body>
      <div class="contain">
         <%- include('../partials/admin/nav'); %>
         <div class="main">
            <h1 class="titlemain" style="float:left">Автори</h1>
            <p id="toolbuttons" style="float:right; padding-top:20px;"><a class="reloadbutton" href="#">Оновити</a><a class="addbutton" hidden="true" href="#">Додати книгу</a></p>
            <div style="clear:both"></div>
            <div class="maincont" style="overflow-x:auto;">
            <% if (data !== null) { %>
               <table>
                  <tr>
                     <th>Автор</th>
                     <th>К-ть книг</th>
                  </tr>
                  <% for(var key in data) { %>
                  <tr>
                     <td><a href="/authors/<%= data[key].id %>"><%= data[key].first_name %> <%= data[key].last_name %></a></td>
                     <td><%= data[key].count %></td>
                  </tr>
                  <% } %>
                </table>
            <% } else { %>
                <p>Немає авторів</p>
            <% } %>
            </div>
         </div>
         <%- include('../partials/footer'); %>
      </div>
      <%- include('../partials/scripts'); %>
   </body>
</html>